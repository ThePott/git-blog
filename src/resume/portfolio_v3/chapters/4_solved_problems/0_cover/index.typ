#import "../../../components/rem/index.typ": rem

#let cover = [
    #pagebreak()
    = Arbor The Tree 진도 관리 시스템으로
    #v(rem(3))
    = 해결한 과제

    #v(rem(2))
    == 1. 쉬운 사용법
    === a. 칸반 진도표
    === b. 그리드 인풋

    #v(rem(2))
    == 2. 병목 현상 해결
    === a. 오답 체크 결과 자동 반영

    #v(rem(2))
    == 3. 성능 최적화
    === a. 서버 사이드 PDF 생성
    === b. 서버 응답 시간 단축
]
