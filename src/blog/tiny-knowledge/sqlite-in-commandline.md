# cli로 sqlite 상호작용

## 기본 명령어

- `sqlite3 [filename]`
    - 시스템에 기본적으로 깔려있다.

## dot(.) 명령어

- `.tables`: list all tables
- `.schema`: show CREATE statements
- `.headers on`: show column headers
- `.mode column`: readable columnar output
- `.exit`: quit
