## 서버랑 연결이 그냥 안 됨

- 발생일: 2025-12-04
- 문제: 카카오 로그아웃을 시도하는데 왜 실패하는지도 모르게 실패함
- 오류

```
(failed)net::ERR_CONNECTION_REFUSED
```

- 원인 파악: `res.status(204)` 라고만 한 게 문제였음
    - 파악 방법: 함수 전체를 주석처리하고 `res` 부분만 남김
- 해결:
    - `res.status(200).send()`
- 소요 시간:
    - 20분
