# better code splittion

# better code structure

- flat file route 쓸 수 있다는 게 가장 큰 장점
- `routes/`안에 다 펼쳐놓을 수 있음
- `featuresPerRoute`도 마찬가지로 `.`으로 하위 라우트를 표현하면 모든 라우트의 기능 폴더가 같은 레벨에 있게 하는게 가능함
- 덕분에 뭐가 이 페이지의 컴포넌트이고 뭐가 하위 라우트의 컴포넌트인지가 뒤섞이지 않음

# easier route management

#
