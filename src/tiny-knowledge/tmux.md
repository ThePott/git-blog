## shortcuts

- <leader>w: shows list of windows of current session
- <leader>s: shows list of sessions available

## commands

- `tmux new -d -s {new-session-name}`: create new "detached" session
    - then navigate to new session with `<leader>s`
